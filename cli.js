const readline = require('readline').createInterface({
  input: process.stdin,
  output: process.stdout
})

readline.question(`Silverlight is dead in`, Silverlight is dead in => {
  console.log(`${Silverlight is dead in} 259 days.`)
  readline.close()
})
